// Placeholder smoke test — the default counter test was removed since
// this app no longer exposes a MyApp widget directly.
// Run integration tests using `flutter test integration_test/` instead.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO: add real smoke tests once Firebase test environment is configured.
    expect(true, isTrue);
  });
}
